{
    "version": "0.0.1",
    "configurations": [
        {
            "name": "funnel",
            "runtimeExecutable": "bash",
            "runtimeArgs": ["-c", "set -a && source .env && set +a && npx vite --port 5173"],
            "port": 5173
        }
    ]
}
